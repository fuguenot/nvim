local surround = {
    'kylechui/nvim-surround',
    version = '^3.0.0',
}

local comment = {
    'numToStr/Comment.nvim',
    opts = {},
}

local autoclose = {
    'm4xshen/autoclose.nvim',
    opts = {},
}

return { surround, comment, autoclose }
